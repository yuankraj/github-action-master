name: Hello
on:
 push:
   branches: [main]

jobs:
  greet:
     runs-on: ubuntu-latest
     
     steps:
      - name: say hello
        run: echo "hello baby"
       
      - name: say username
        run: echo "$USER"

 
